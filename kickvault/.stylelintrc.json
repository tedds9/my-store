{
  "extends": [
    "stylelint-config-standard"
  ],
  "rules": {
    "import-notation": "string",
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["layer", "import"]
      }
    ],
    "selector-class-pattern": [
      "^[a-z][a-zA-Z0-9]*$",
      {
        "message": "ARCHITECTURAL ERROR: Selector must strictly match the modern camelCase specification pattern for CSS Modules."

      }
    ],
    "no-descending-specificity": [
      true,
      {
        "severity": "error",
        "message": "REGRESSION WARNING: Detected descending specificity. Fix stylesheet cascade ordering to prevent layout engine leaks."
      }
    ]
  }
}
